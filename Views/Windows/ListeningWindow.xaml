<Window x:Class="TDEduEnglish.Views.Windows.ListeningWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TDEduEnglish.Views.Windows"
        mc:Ignorable="d"
        Title="ListeningWindow" Height="800" Width="1200">
    <Window.Resources>
        <!-- Compact Button Style -->
        <Style x:Key="CompactButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF6366F1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF5855E8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF4F46E5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Small Icon Button Style -->
        <Style x:Key="SmallIconButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FFF3F4F6"/>
            <Setter Property="Foreground" Value="#FF6B7280"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                Padding="6">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF6366F1"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF4F46E5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Answer Option Style -->
        <Style x:Key="AnswerOptionStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#FF374151"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#FFE5E7EB"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" 
                                       Width="20" Height="20" 
                                       CornerRadius="10"
                                       BorderThickness="2"
                                       BorderBrush="#FFD1D5DB"
                                       Background="Transparent"
                                       Margin="0,0,12,0">
                                    <Ellipse x:Name="CheckMark" 
                                            Width="10" Height="10"
                                            Fill="#FF6366F1"
                                            Visibility="Collapsed"/>
                                </Border>

                                <ContentPresenter Grid.Column="1"
                                                 Content="{TemplateBinding Content}"
                                                 VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FFF9FAFB"/>
                                <Setter Property="BorderBrush" Value="#FF6366F1"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Background" Value="#FFEFF6FF"/>
                                <Setter Property="BorderBrush" Value="#FF6366F1"/>
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Question Card Style -->
        <Style x:Key="QuestionCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="24"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Gray" Direction="270" ShadowDepth="2" BlurRadius="8" Opacity="0.08"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Fixed Header Section -->
        <Grid Height="140" VerticalAlignment="Top" Panel.ZIndex="100">
            <Grid.Background>
                <SolidColorBrush Color="White" Opacity="0.98"/>
            </Grid.Background>
            <Grid.Effect>
                <DropShadowEffect Color="Gray" Direction="270" ShadowDepth="1" BlurRadius="12" Opacity="0.1"/>
            </Grid.Effect>

            <Grid Margin="24,16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Top Row: Title and Timer -->
                <Grid Grid.Row="0" Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Title Section -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Background="#FF6366F1" CornerRadius="8" Width="32" Height="32" Margin="0,0,12,0">
                            <TextBlock Text="🎧" FontSize="16" 
                                      HorizontalAlignment="Center" 
                                      VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="{Binding Title}" 
                                      FontSize="18" FontWeight="SemiBold" 
                                      Foreground="#FF1F2937"/>
                            <TextBlock Text="{Binding Level}" 
                                      FontSize="12" Foreground="#FF6B7280"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Timer Section -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Background="#FFFEF3C7" CornerRadius="8" Padding="12,6" Margin="0,0,12,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="⏰" FontSize="16" Margin="0,0,8,0"/>
                                <TextBlock Text="{Binding RemainingTime, StringFormat=mm\\:ss}" 
                                          FontSize="16" FontWeight="Bold" 
                                          Foreground="#FF92400E"/>
                            </StackPanel>
                        </Border>

                        <Button Content="⏸️" 
                               Command="{Binding PauseTimerCommand}"
                               Style="{StaticResource SmallIconButtonStyle}"
                               ToolTip="Pause Timer"/>
                    </StackPanel>
                </Grid>

                <!-- Audio Controls Row -->
                <Border Grid.Row="1" Background="#FFF9FAFB" CornerRadius="10" Padding="16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Play/Pause Button -->
                        <Button Grid.Column="0" 
                               Content="{Binding IsPlaying}"
                               Command="{Binding PlayPauseCommand}"
                               Style="{StaticResource SmallIconButtonStyle}"
                               Width="44" Height="44"
                               FontSize="18"
                               Margin="0,0,16,0"/>

                        <!-- Progress Bar -->
                        <Grid Grid.Column="1" VerticalAlignment="Center">
                            <Slider x:Name="AudioProgressSlider"
                                   Value="{Binding CurrentPosition}"
                                   Maximum="{Binding TotalDuration}"
                                   IsEnabled="{Binding IsAudioLoaded}"
                                   Height="24"
                                   Style="{StaticResource {x:Type Slider}}"/>

                            <StackPanel Orientation="Horizontal" 
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Margin="0,0,0,0">
                                <TextBlock Text="{Binding CurrentPositionText}" 
                                          FontSize="11" Foreground="#FF6B7280"/>
                                <TextBlock Text=" / " 
                                          FontSize="11" Foreground="#FF6B7280"/>
                                <TextBlock Text="{Binding TotalDurationText}" 
                                          FontSize="11" Foreground="#FF6B7280"/>
                            </StackPanel>
                        </Grid>

                        <!-- Volume and Controls -->
                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                            <Button Content="🔄" 
                                   Command="{Binding RestartAudioCommand}"
                                   Style="{StaticResource SmallIconButtonStyle}"
                                   ToolTip="Restart Audio"
                                   Margin="0,0,8,0"/>

                            <TextBlock Text="🔊" FontSize="16" 
                                      VerticalAlignment="Center" 
                                      Margin="0,0,8,0"/>

                            <Slider Value="{Binding Volume}" 
                                   Minimum="0" Maximum="1" 
                                   Width="80" Height="24"
                                   VerticalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Grid>

        <!-- Questions Section -->
        <ScrollViewer Margin="0,140,0,80" 
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Disabled"
                     Background="Transparent">
            <StackPanel Margin="24,16,24,24">
                <ItemsControl ItemsSource="{Binding Questions}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource QuestionCardStyle}">
                                <StackPanel>
                                    <!-- Question Header -->
                                    <Grid Margin="0,0,0,20">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <Border Grid.Column="0" 
                                               Background="#FF6366F1" 
                                               CornerRadius="6" 
                                               Width="28" Height="28"
                                               Margin="0,0,12,0">
                                            <TextBlock Text="{Binding QuestionNumber}" 
                                                      FontSize="14" FontWeight="Bold"
                                                      Foreground="White"
                                                      HorizontalAlignment="Center" 
                                                      VerticalAlignment="Center"/>
                                        </Border>

                                        <TextBlock Grid.Column="1" 
                                                  Text="{Binding QuestionText}" 
                                                  FontSize="16" FontWeight="Medium"
                                                  Foreground="#FF1F2937"
                                                  TextWrapping="Wrap"
                                                  VerticalAlignment="Center"/>

                                        <Border Grid.Column="2" 
                                               Background="{Binding IsAnswered}"
                                               CornerRadius="4" 
                                               Padding="8,4">
                                            <TextBlock Text="{Binding IsAnswered}" 
                                                      FontSize="11" FontWeight="Medium"
                                                      Foreground="White"/>
                                        </Border>
                                    </Grid>

                                    <!-- Answer Options -->
                                    <StackPanel Margin="0,0,0,0">
                                        <RadioButton Content="{Binding Option1}" 
                                                    IsChecked="{Binding IsOption1Selected}"
                                                    Style="{StaticResource AnswerOptionStyle}"/>

                                        <RadioButton Content="{Binding Option2}" 
                                                    IsChecked="{Binding IsOption2Selected}"
                                                    Style="{StaticResource AnswerOptionStyle}"/>

                                        <RadioButton Content="{Binding Option3}" 
                                                    IsChecked="{Binding IsOption3Selected}"
                                                    Style="{StaticResource AnswerOptionStyle}"/>

                                        <RadioButton Content="{Binding Option4}" 
                                                    IsChecked="{Binding IsOption4Selected}"
                                                    Style="{StaticResource AnswerOptionStyle}"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>

        <!-- Fixed Bottom Section -->
        <Border Height="80" VerticalAlignment="Bottom" 
               Background="White" 
               BorderThickness="0,1,0,0"
               BorderBrush="#FFE5E7EB"
               Panel.ZIndex="100">
            <Border.Effect>
                <DropShadowEffect Color="Gray" Direction="90" ShadowDepth="1" BlurRadius="8" Opacity="0.08"/>
            </Border.Effect>

            <Grid Margin="24,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Progress Info -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="#FFECFDF5" CornerRadius="6" Padding="12,6" Margin="0,0,16,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📝" FontSize="14" Margin="0,0,6,0"/>
                            <TextBlock FontSize="13" Foreground="#FF065F46">
                                <Run Text="{Binding AnsweredCount}"/>
                                <Run Text="/"/>
                                <Run Text="{Binding TotalQuestions}"/>
                                <Run Text="answered"/>
                            </TextBlock>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="{Binding CompletionMessage}" 
                              FontSize="13" Foreground="#FF6B7280"
                              VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Submit Button -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="Review Answers" 
                           Command="{Binding ReviewAnswersCommand}"
                           Style="{StaticResource CompactButtonStyle}"
                           Background="Transparent"
                           Foreground="#FF6366F1"
                           BorderThickness="1"
                           BorderBrush="#FF6366F1"
                           Margin="0,0,12,0"/>

                    <Button Content="Submit Test" 
                           Command="{Binding SubmitCommand}"
                           Style="{StaticResource CompactButtonStyle}"
                           IsEnabled="{Binding CanSubmit}"
                           FontWeight="Bold"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
