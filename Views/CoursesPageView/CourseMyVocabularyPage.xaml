<Page x:Class="TDEduEnglish.Views.CoursesPageView.CourseMyVocabularyPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TDEduEnglish.Views.CoursesPageView"
      mc:Ignorable="d" 
      Background="White"
      d:DesignHeight="740" d:DesignWidth="1200"
      Title="CourseMyVocabularyPage">

    <Page.Resources>
        <!-- Compact Button Style -->
        <Style x:Key="CompactButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF6366F1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF5855E8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF4F46E5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Icon Button Style -->
        <Style x:Key="IconButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FF6B7280"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                Padding="4">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FFF3F4F6"/>
                                <Setter Property="Foreground" Value="#FF6366F1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Delete Button Style -->
        <Style x:Key="DeleteButtonStyle" TargetType="Button" BasedOn="{StaticResource IconButtonStyle}">
            <Setter Property="Foreground" Value="#FFEF4444"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFFEF2F2"/>
                    <Setter Property="Foreground" Value="#FFDC2626"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Card Style -->
        <Style x:Key="VocabCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Gray" Direction="270" 
                                      ShadowDepth="2" BlurRadius="10" Opacity="0.06"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Border Grid.Row="0" Background="White" Padding="40,32">
            <Border.Effect>
                <DropShadowEffect Color="Gray" Direction="270" 
                                  ShadowDepth="1" BlurRadius="10" Opacity="0.05"/>
            </Border.Effect>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="📖 Từ vựng đã lưu" 
                              FontSize="32" FontWeight="Bold" 
                              Foreground="#FF1F2937" Margin="0,0,0,8"/>
                    <TextBlock Text="Ôn tập và quản lý danh sách từ vựng của bạn" 
                              FontSize="15" Foreground="#FF6B7280"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="{Binding TotalWords, StringFormat='Tổng: {0} từ'}" 
                              FontSize="14" FontWeight="SemiBold"
                              Foreground="#FF6366F1" 
                              VerticalAlignment="Center"
                              Margin="0,0,20,0"/>

                    <Button Content="🔄 Làm mới" 
                           Command="{Binding RefreshCommand}"
                           Style="{StaticResource CompactButtonStyle}"
                           Margin="0,0,12,0"/>

                    <Button Content="➕ Thêm từ mới" 
                           Command="{Binding AddNewWordCommand}"
                           Style="{StaticResource CompactButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content Section -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" 
                     Padding="40,24">
            <ItemsControl ItemsSource="{Binding UserVocabularies}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource VocabCardStyle}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Vocabulary Content -->
                                <Grid Grid.Column="0">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- Word and Type -->
                                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                                        <TextBlock Text="{Binding Word}" 
                                                  FontSize="24" FontWeight="Bold" 
                                                  Foreground="#FF1F2937"
                                                  VerticalAlignment="Center"/>

                                        <Border Background="#FFDBEAFE" 
                                               CornerRadius="6" 
                                               Padding="8,4" Margin="12,0,0,0"
                                               VerticalAlignment="Center">
                                            <TextBlock Text="{Binding WordType}" 
                                                      FontSize="12" FontWeight="SemiBold"
                                                      Foreground="#FF6366F1"/>
                                        </Border>
                                    </StackPanel>

                                    <!-- IPA -->
                                    <TextBlock Grid.Row="1" 
                                              Text="{Binding IPATranscription, StringFormat='/{0}/'}" 
                                              FontSize="16" FontStyle="Italic"
                                              Foreground="#FF6B7280"
                                              Margin="0,0,0,12"/>

                                    <!-- Meaning -->
                                    <Border Grid.Row="2" Background="#FFF9FAFB" 
                                           CornerRadius="8" Padding="16,12">
                                        <StackPanel>
                                            <TextBlock Text="Nghĩa:" 
                                                      FontSize="12" FontWeight="SemiBold"
                                                      Foreground="#FF6B7280" Margin="0,0,0,4"/>
                                            <TextBlock Text="{Binding Meaning}" 
                                                      FontSize="15" 
                                                      Foreground="#FF1F2937"
                                                      TextWrapping="Wrap"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>

                                <!-- Action Buttons -->
                                <StackPanel Grid.Column="1" Orientation="Horizontal" 
                                           VerticalAlignment="Top" Margin="16,0,0,0">
                                    <Button ToolTip="Phát âm" 
                                           Command="{Binding DataContext.PlayPronunciationCommand, 
                                                    RelativeSource={RelativeSource AncestorType=Page}}"
                                           CommandParameter="{Binding Word}"
                                           Style="{StaticResource IconButtonStyle}"
                                           Margin="0,0,8,0">
                                        <TextBlock Text="🔊" FontSize="16"/>
                                    </Button>

                                    <Button ToolTip="Chỉnh sửa" 
                                           Command="{Binding DataContext.EditWordCommand, 
                                                    RelativeSource={RelativeSource AncestorType=Page}}"
                                           CommandParameter="{Binding}"
                                           Style="{StaticResource IconButtonStyle}"
                                           Margin="0,0,8,0">
                                        <TextBlock Text="✏️" FontSize="16"/>
                                    </Button>

                                    <Button ToolTip="Xóa" 
                                           Command="{Binding DataContext.DeleteWordCommand, 
                                                    RelativeSource={RelativeSource AncestorType=Page}}"
                                           CommandParameter="{Binding}"
                                           Style="{StaticResource DeleteButtonStyle}">
                                        <TextBlock Text="🗑️" FontSize="16"/>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Empty State -->
        <StackPanel Grid.Row="1" 
                   VerticalAlignment="Center" HorizontalAlignment="Center"
                   Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibilityConverter}}">
            <TextBlock Text="📚" FontSize="72" 
                      HorizontalAlignment="Center" Margin="0,0,0,16"/>
            <TextBlock Text="Chưa có từ vựng nào" 
                      FontSize="24" FontWeight="SemiBold"
                      Foreground="#FF1F2937" 
                      HorizontalAlignment="Center" Margin="0,0,0,8"/>
            <TextBlock Text="Hãy bắt đầu thêm từ vựng để học tập hiệu quả hơn" 
                      FontSize="15" Foreground="#FF6B7280"
                      HorizontalAlignment="Center" Margin="0,0,0,24"/>
            <Button Content="➕ Thêm từ đầu tiên" 
                   Command="{Binding AddNewWordCommand}"
                   Style="{StaticResource CompactButtonStyle}"
                   Padding="20,10" FontSize="15"/>
        </StackPanel>
    </Grid>
</Page>
