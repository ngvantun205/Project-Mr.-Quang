<Page x:Class="TDEduEnglish.Views.CoursesPageView.CourseListeningListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TDEduEnglish.Views.CoursesPageView"
      Background="AliceBlue"
      mc:Ignorable="d" 
      d:DesignHeight="740" d:DesignWidth="1200"
      Title="CourseListeningListPage">

    <Page.Resources>
        <!-- Reading Card Style -->
        <Style x:Key="ReadingCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Gray" Direction="270" ShadowDepth="2" BlurRadius="10" Opacity="0.08"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Reading Card Hover Style -->
        <Style x:Key="ReadingCardHoverStyle" TargetType="Border" BasedOn="{StaticResource ReadingCardStyle}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#FF6366F1" Direction="270" ShadowDepth="4" BlurRadius="15" Opacity="0.2"/>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Background" Value="#FFFAFBFC"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Tab Header Style -->
        <Style x:Key="ModernTabItemStyle" TargetType="TabItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FF6B7280"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="Margin" Value="0,0,4,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Border" Background="{TemplateBinding Background}"
                                CornerRadius="8,8,0,0"
                                Padding="{TemplateBinding Padding}"
                                BorderThickness="0,0,0,2"
                                BorderBrush="Transparent">
                            <ContentPresenter ContentSource="Header"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FFF8FAFC"/>
                                <Setter Property="Foreground" Value="#FF6366F1"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="White"/>
                                <Setter Property="Foreground" Value="#FF6366F1"/>
                                <Setter Property="Panel.ZIndex" Value="1"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#FF6366F1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Level Badge Style -->
        <Style x:Key="LevelBadgeStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>

        <!-- Start Button Style -->
        <Style x:Key="StartButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF6366F1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF5855E8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF4F46E5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#FFF8FAFC" Offset="0"/>
                <GradientStop Color="#FFF1F5F9" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <!-- Page Header -->
        <StackPanel Margin="40,20,40,0" VerticalAlignment="Top">
            <TextBlock Text="📖 Listening Practice" 
                       FontSize="32" FontWeight="Bold" 
                       Foreground="#FF1F2937" 
                       Margin="0,0,0,8"/>
            <TextBlock Text="Improve your listening comprehension with engaging articles and stories" 
                       FontSize="16" 
                       Foreground="#FF6B7280" 
                       Margin="0,0,0,30"/>
        </StackPanel>

        <!-- TabControl for Levels -->
        <TabControl Margin="40,120,40,40" 
                    Background="Transparent" 
                    BorderThickness="0"
                    TabStripPlacement="Top">
            <TabControl.Resources>
                <Style TargetType="TabPanel">
                    <Setter Property="HorizontalAlignment" Value="Left"/>
                    <Setter Property="Margin" Value="0,0,0,20"/>
                </Style>
            </TabControl.Resources>

            <!-- Beginner Tab -->
            <TabItem Header="🌱 Beginner (A1-A2)" Style="{StaticResource ModernTabItemStyle}">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,20,0,0">
                    <ItemsControl ItemsSource="{Binding BeginnerListenings}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="3"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource ReadingCardHoverStyle}"
                                        Tag="{Binding}"
                                        Height="200">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <!-- Level Badge -->
                                        <Border Grid.Row="0" Style="{StaticResource LevelBadgeStyle}"
                                                Background="#FF10B981" Margin="0,0,0,8">
                                            <TextBlock Text="{Binding Level}" 
                                                       Foreground="White" 
                                                       FontSize="10" 
                                                       FontWeight="SemiBold"/>
                                        </Border>

                                        <!-- Title -->
                                        <TextBlock Grid.Row="1" 
                                                   Text="{Binding Title}" 
                                                   FontSize="14" 
                                                   FontWeight="SemiBold" 
                                                   Foreground="#FF1F2937"
                                                   TextWrapping="Wrap"
                                                   LineHeight="18"
                                                   MaxHeight="54"
                                                   TextTrimming="WordEllipsis"
                                                   Margin="0,0,0,8"/>

                                        <!-- Bottom Info -->
                                        <StackPanel Grid.Row="2" Orientation="Horizontal" 
                                                   VerticalAlignment="Center" Margin="0,0,0,8">
                                            <TextBlock Text="⏱️" FontSize="11" Margin="0,0,3,0"/>
                                            <TextBlock FontSize="11" 
                                                       Foreground="#FF6B7280"
                                                       Margin="0,0,8,0">
                                                <TextBlock.Text>
                                                    <Binding Path="SuggestedTime" StringFormat="{}{0:mm}min"/>
                                                </TextBlock.Text>
                                            </TextBlock>
                                            <TextBlock Text="❓" FontSize="11" Margin="0,0,3,0"/>
                                            <TextBlock Text="{Binding Questions.Count}" 
                                                       FontSize="11" 
                                                       Foreground="#FF6B7280"/>
                                            <TextBlock Text="q" 
                                                       FontSize="11" 
                                                       Foreground="#FF6B7280"/>
                                        </StackPanel>

                                        <!-- Start Button -->
                                        <Button Grid.Row="3" 
                                                Content="Start Reading" 
                                                Style="{StaticResource StartButtonStyle}"
                                                FontSize="11"
                                                Height="32"
                                                Command="{Binding DataContext.StartListeningCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                CommandParameter="{Binding}"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </TabItem>

            <!-- Intermediate Tab -->
            <TabItem Header="🚀 Intermediate (B1-B2)" Style="{StaticResource ModernTabItemStyle}">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,20,0,0">
                    <ItemsControl ItemsSource="{Binding IntermediateListenings}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="3"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource ReadingCardHoverStyle}"
                                        Tag="{Binding}"
                                        Height="200">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <Border Grid.Row="0" Style="{StaticResource LevelBadgeStyle}"
                                                Background="#FF6366F1" Margin="0,0,0,8">
                                            <TextBlock Text="{Binding Level}" 
                                                       Foreground="White" 
                                                       FontSize="10" 
                                                       FontWeight="SemiBold"/>
                                        </Border>

                                        <TextBlock Grid.Row="1" 
                                                   Text="{Binding Title}" 
                                                   FontSize="14" 
                                                   FontWeight="SemiBold" 
                                                   Foreground="#FF1F2937"
                                                   TextWrapping="Wrap"
                                                   LineHeight="18"
                                                   MaxHeight="54"
                                                   TextTrimming="WordEllipsis"
                                                   Margin="0,0,0,8"/>

                                        <StackPanel Grid.Row="2" Orientation="Horizontal" 
                                                   VerticalAlignment="Center" Margin="0,0,0,8">
                                            <TextBlock Text="⏱️" FontSize="11" Margin="0,0,3,0"/>
                                            <TextBlock FontSize="11" 
                                                       Foreground="#FF6B7280"
                                                       Margin="0,0,8,0">
                                                <TextBlock.Text>
                                                    <Binding Path="SuggestedTime" StringFormat="{}{0:mm}min"/>
                                                </TextBlock.Text>
                                            </TextBlock>
                                            <TextBlock Text="❓" FontSize="11" Margin="0,0,3,0"/>
                                            <TextBlock Text="{Binding Questions.Count}" 
                                                       FontSize="11" 
                                                       Foreground="#FF6B7280"/>
                                            <TextBlock Text="q" 
                                                       FontSize="11" 
                                                       Foreground="#FF6B7280"/>
                                        </StackPanel>

                                        <Button Grid.Row="3" 
                                                Content="Start Reading" 
                                                Style="{StaticResource StartButtonStyle}"
                                                FontSize="11"
                                                Height="32"
                                                Command="{Binding DataContext.StartListeningCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                CommandParameter="{Binding}"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </TabItem>

            <!-- Advanced Tab -->
            <TabItem Header="🎯 Advanced (C1-C2)" Style="{StaticResource ModernTabItemStyle}">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,20,0,0">
                    <ItemsControl ItemsSource="{Binding AdvancedListenings}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="3"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource ReadingCardHoverStyle}"
                                        Tag="{Binding}"
                                        Height="200">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <Border Grid.Row="0" Style="{StaticResource LevelBadgeStyle}"
                                                Background="#FFEF4444" Margin="0,0,0,8">
                                            <TextBlock Text="{Binding Level}" 
                                                       Foreground="White" 
                                                       FontSize="10" 
                                                       FontWeight="SemiBold"/>
                                        </Border>

                                        <TextBlock Grid.Row="1" 
                                                   Text="{Binding Title}" 
                                                   FontSize="14" 
                                                   FontWeight="SemiBold" 
                                                   Foreground="#FF1F2937"
                                                   TextWrapping="Wrap"
                                                   LineHeight="18"
                                                   MaxHeight="54"
                                                   TextTrimming="WordEllipsis"
                                                   Margin="0,0,0,8"/>

                                        <StackPanel Grid.Row="2" Orientation="Horizontal" 
                                                   VerticalAlignment="Center" Margin="0,0,0,8">
                                            <TextBlock Text="⏱️" FontSize="11" Margin="0,0,3,0"/>
                                            <TextBlock FontSize="11" 
                                                       Foreground="#FF6B7280"
                                                       Margin="0,0,8,0">
                                                <TextBlock.Text>
                                                    <Binding Path="SuggestedTime" StringFormat="{}{0:mm}min"/>
                                                </TextBlock.Text>
                                            </TextBlock>
                                            <TextBlock Text="❓" FontSize="11" Margin="0,0,3,0"/>
                                            <TextBlock Text="{Binding Questions.Count}" 
                                                       FontSize="11" 
                                                       Foreground="#FF6B7280"/>
                                            <TextBlock Text="q" 
                                                       FontSize="11" 
                                                       Foreground="#FF6B7280"/>
                                        </StackPanel>

                                        <Button Grid.Row="3" 
                                                Content="Start Reading" 
                                                Style="{StaticResource StartButtonStyle}"
                                                FontSize="11"
                                                Height="32"
                                                Command="{Binding DataContext.StartListeningCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                CommandParameter="{Binding}"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </TabItem>
        </TabControl>
    </Grid>
</Page>
