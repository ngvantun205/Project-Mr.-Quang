<Window x:Class="TDEduEnglish.Views.SuperAdminWindow.ManageVocabularyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TDEduEnglish.Views.SuperAdminWindow"
        mc:Ignorable="d"
        Title="ManageVocabularyWindow" Height="800" Width="1200">
    <Window.Resources>
        <!-- Reuse Color Scheme -->
        <SolidColorBrush x:Key="PrimaryColor" Color="#FF6366F1"/>
        <SolidColorBrush x:Key="LightBackground" Color="White"/>
        <SolidColorBrush x:Key="TextDark" Color="#FF1F2937"/>
        <SolidColorBrush x:Key="TextGray" Color="#FF6B7280"/>

        <!-- Button Styles -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="4,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF5855E8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF4F46E5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SmallButton" TargetType="Button">
            <Setter Property="Background" Value="#FF6366F1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="10">
                            <Border.Effect>
                                <DropShadowEffect Color="#FF6366F1" Direction="270" ShadowDepth="2" BlurRadius="8" Opacity="0.3"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF5855E8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Card -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource LightBackground}"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Gray" Direction="270" ShadowDepth="2" BlurRadius="10" Opacity="0.08"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DataGrid -->
        <Style x:Key="ModernDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="RowHeight" Value="40"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="False"/>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" VerticalAlignment="Center" Margin="0,0,0,16">
            <TextBlock Text="📚 Vocabulary Management"
                       FontSize="24" FontWeight="Bold"
                       Foreground="{StaticResource TextDark}" Margin="0,0,12,0"/>
            <TextBlock Text="Manage words and meanings easily"
                       FontSize="14" Foreground="{StaticResource TextGray}"
                       VerticalAlignment="Bottom"/>
        </StackPanel>

        <!-- Toolbar -->
        <Border Grid.Row="1" Style="{StaticResource Card}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Search -->
                <TextBox Grid.Column="0" Height="32" Margin="0,0,8,0"
                         Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"
                         VerticalContentAlignment="Center"
                         FontSize="14"
                         ToolTip="Search by word or meaning"/>

                <!-- Filter Level -->
                <ComboBox Grid.Column="1" Margin="0,0,8,0" Height="32" Width="200"
                          ItemsSource="{Binding Level}" 
                          SelectedItem="{Binding SelectedLevel}"
                          FontSize="14"
                          ToolTip="Filter by Level"/>

                <!-- Filter Topic -->
                <ComboBox Grid.Column="2" Margin="0,0,8,0" Height="32" Width="200"
                          ItemsSource="{Binding Topic}"
                          SelectedItem="{Binding SelectedTopic}"
                          FontSize="14"
                          ToolTip="Filter by Topic"/>

                <!-- Buttons -->
                <StackPanel Grid.Column="3" Orientation="Horizontal">
                    <Button Content="Filter" Width="60" Command="{Binding FilterCommand}" Style="{StaticResource SmallButton}"/>
                    <Button Content="Add" Width="50" Command="{Binding AddCommand}" Style="{StaticResource SmallButton}"/>
                    <Button Content="Update" Width="60" Command="{Binding UpdateCommand}" Style="{StaticResource SmallButton}"/>
                    <Button Content="Delete" Width="60" Command="{Binding DeleteCommand}" Style="{StaticResource SmallButton}"/>
                    <Button Content="Import Json" Width="80" Command="{Binding ImportFromJsonCommand}" Style="{StaticResource SmallButton}"/>

                </StackPanel>
            </Grid>
        </Border>

        <!-- DataGrid -->
        <Border Grid.Row="2" Style="{StaticResource Card}">
            <DataGrid ItemsSource="{Binding VocabularyList}" 
                      SelectedItem="{Binding SelectedVocabulary}" 
                      Style="{StaticResource ModernDataGrid}">
            </DataGrid>
        </Border>
    </Grid>
</Window>
