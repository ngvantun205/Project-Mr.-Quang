<Window x:Class="TDEduEnglish.Views.SuperAdminWindow.ManageUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TDEduEnglish.Views.SuperAdminWindow"
        mc:Ignorable="d"
        Title="ManageUserWindow" Height="700" Width="1100">
    <Window.Resources>
        <!-- Card Style -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Gray" Direction="270" ShadowDepth="2" BlurRadius="10" Opacity="0.08"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary Button -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF6366F1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF5855E8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Button -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#FF4B5563"/>
            <Setter Property="BorderBrush" Value="#FFE5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FFF9FAFB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DataGrid Style -->
        <Style x:Key="ModernDataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="RowHeight" Value="40"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="False"/>
            <Setter Property="Margin" Value="0,8,0,0"/>
        </Style>

        <Style x:Key="DataGridHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#FFF3F4F6"/>
            <Setter Property="Foreground" Value="#FF4B5563"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,16">
            <TextBlock Text="👥 User Management" 
                       FontSize="22" FontWeight="Bold" 
                       Foreground="#FF1F2937" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,0">
                
                <Button Content="Refresh" 
                        Command="{Binding RefreshCommand}"
                        Style="{StaticResource SecondaryButtonStyle}" Margin="8,0,0,0"/>
            </StackPanel>
        </StackPanel>

        <!-- Main Card -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Search & Filters -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBox Width="220" Height="30" 
                             Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}" 
                             VerticalContentAlignment="Center"
                             BorderBrush="#FFE5E7EB" BorderThickness="1"
                             Margin="0,0,8,0" />
                    <Button Content="Search"
                            Command="{Binding SearchCommand}"
                            Style="{StaticResource SecondaryButtonStyle}"/>
                    <Button Content="Add User" 
                        Command="{Binding AddUserCommand}"
                        Style="{StaticResource PrimaryButtonStyle}" Margin="16,0,0,0"/>
                    <Button Content="Delete User" 
                        Command="{Binding DeleteUserCommand}"
                        Style="{StaticResource PrimaryButtonStyle}" Margin="16,0,0,0"/>
                    <Button Content="Update User" 
                        Command="{Binding UpdateUserCommand}"
                        Style="{StaticResource PrimaryButtonStyle}" Margin="16,0,0,0"/>
                </StackPanel>

                <!-- User DataGrid -->
                <DataGrid Grid.Row="1" Style="{StaticResource ModernDataGridStyle}"
                          ItemsSource="{Binding Users}"
                          SelectedItem="{Binding SelectedUser, Mode=TwoWay}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding UserId}" 
                                            HeaderStyle="{StaticResource DataGridHeaderStyle}" Width="60"/>
                        <DataGridTextColumn Header="Full Name" Binding="{Binding FullName}" 
                                            HeaderStyle="{StaticResource DataGridHeaderStyle}" Width="200"/>
                        <DataGridTextColumn Header="Email" Binding="{Binding Email}" 
                                            HeaderStyle="{StaticResource DataGridHeaderStyle}" Width="200"/>
                        <DataGridTextColumn Header="Role" Binding="{Binding Role}" 
                                            HeaderStyle="{StaticResource DataGridHeaderStyle}" Width="100"/>
                        <DataGridTextColumn Header="Phone Number" Binding="{Binding PhoneNumber}" 
                                            HeaderStyle="{StaticResource DataGridHeaderStyle}"  Width="150"/>
                        <DataGridTextColumn Header="Join Date" Binding="{Binding JoinDate}" 
                                            HeaderStyle="{StaticResource DataGridHeaderStyle}" Width="120"/>
                        <DataGridTextColumn Header="Date of Birth" Binding="{Binding DateOfBirth}" 
                                            HeaderStyle="{StaticResource DataGridHeaderStyle}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Pagination -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,16,0,0">
                    <Button Content="⟨ Prev" Command="{Binding PrevPageCommand}" 
                            Style="{StaticResource SecondaryButtonStyle}" Margin="4,0"/>
                    <TextBlock Text="{Binding PageInfo}" FontSize="13" VerticalAlignment="Center" Margin="8,0"/>
                    <Button Content="Next ⟩" Command="{Binding NextPageCommand}" 
                            Style="{StaticResource SecondaryButtonStyle}" Margin="4,0"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
