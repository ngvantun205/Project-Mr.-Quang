<Window x:Class="TDEduEnglish.Views.SuperAdminWindow.ManageListeningWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TDEduEnglish.Views.SuperAdminWindow"
        mc:Ignorable="d"
        Title="ManageListeningWindow" Height="800" Width="1200">
    <Window.Resources>
        <!-- Small reusable styles -->
        <Style x:Key="SmallPrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#FF6366F1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="6,0,0,0"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Width" Value="90"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF5855E8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SmallIconButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="CardBorder" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Gray" Direction="270" ShadowDepth="1" BlurRadius="8" Opacity="0.06"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGrid" x:Key="CompactDataGrid">
            <Setter Property="RowHeight" Value="36"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
        </Style>
    </Window.Resources>

    <Grid Background="#FFF7F8FB" Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" VerticalAlignment="Center" Margin="6">
            <TextBlock Text="Listening Lessons Management" FontSize="20" FontWeight="Bold" Foreground="#FF1F2937"/>
            <TextBlock Text="  •  Manage lessons annd questions" Foreground="#FF6B7280" VerticalAlignment="Center" Margin="8,0,0,0"/>
        </StackPanel>

        <!-- Controls: Search / Filter / Import / CRUD -->
        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Margin="0,12,0,12">
            <DockPanel LastChildFill="False">
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center">
                    <TextBlock Text="Search:" VerticalAlignment="Center" Margin="6" />
                    <TextBox Width="260" Margin="6,0" 
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Text="Level:" VerticalAlignment="Center" Margin="6,0,0,0"/>
                    <ComboBox Width="140" Margin="6,0"
                              ItemsSource="{Binding AvailableLevels}" 
                              SelectedItem="{Binding SelectedLevel}" />

                    <Button Style="{StaticResource SmallPrimaryButton}" Command="{Binding ClearFilterCommand}" Margin="12,0">Clear</Button>
                </StackPanel>

                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" VerticalAlignment="Center">
                    <Button Content="Import JSON" Style="{StaticResource SmallPrimaryButton}" Command="{Binding ImportLessonFromJsonCommand}"/>
                    <Button Content="Add Lesson" Style="{StaticResource SmallPrimaryButton}" Command="{Binding AddLessonCommand}"/>
                    <Button Content="Edit" Style="{StaticResource SmallPrimaryButton}" Command="{Binding EditLessonCommand}" />
                    <Button Content="Delete" Style="{StaticResource SmallPrimaryButton}" Command="{Binding DeleteLessonCommand}" />
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Main area: left = lessons grid, right = lesson details & questions -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="470*"/>
                <ColumnDefinition Width="22*"/>
                <ColumnDefinition Width="683*"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Lessons list -->
            <Border Grid.Column="0" Style="{StaticResource CardBorder}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Listening Lessons" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,8"/>

                    <DataGrid Grid.Row="1" Style="{StaticResource CompactDataGrid}"
                              ItemsSource="{Binding ListeningLessons}"
                              SelectedItem="{Binding SelectedLesson, Mode=TwoWay}"
                              SelectionMode="Single">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="#" Binding="{Binding ListeningLessonId}" Width="50"/>
                            <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*"/>
                            <DataGridTextColumn Header="Level" Binding="{Binding Level}" Width="120"/>
                            <DataGridTextColumn Header="Time" Binding="{Binding SuggestedTime}" Width="120"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <!-- Right: Selected lesson details + questions -->
            <Border Style="{StaticResource CardBorder}" Grid.ColumnSpan="3" Margin="470,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Lesson header & audio -->
                    <StackPanel Orientation="Horizontal" Grid.Row="0" VerticalAlignment="Center" Margin="0,0,0,8">
                        <TextBlock Text="Lesson Details" FontWeight="SemiBold" FontSize="14"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,0">
                            <Button Content="Save" Style="{StaticResource SmallPrimaryButton}" Command="{Binding SaveLessonCommand}" />
                        </StackPanel>
                    </StackPanel>

                    <!-- Form (no placeholders) -->
                    <UniformGrid Grid.Row="1" Columns="2" Margin="0,0,0,8" HorizontalAlignment="Stretch">
                        <StackPanel>
                            <TextBlock Text="Title" />
                            <TextBox Text="{Binding EditingLesson.Title, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBlock Text="Level" Margin="0,6,0,0"/>
                            <ComboBox ItemsSource="{Binding AvailableLevels}" SelectedItem="{Binding EditingLesson.Level}" />
                        </StackPanel>

                        <StackPanel>
                            <TextBlock Text="Suggested Time (hh:mm:ss)" />
                            <TextBox Text="{Binding EditingLesson.SuggestedTimeString, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBlock Text="Audio Path" Margin="0,6,0,0"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBox Text="{Binding EditingLesson.ListeningAudioPath}" Width="240" />
                                <Button Style="{StaticResource SmallIconButton}" Command="{Binding BrowseAudioCommand}" ToolTip="Browse">
                                    <TextBlock Text="⋯" FontSize="16" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                </Button>
                            </StackPanel>
                        </StackPanel>
                    </UniformGrid>

                    <!-- Questions management -->
                    <Grid Grid.Row="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel Orientation="Horizontal" Grid.Row="0" HorizontalAlignment="Left" Margin="0,0,0,8">
                            <TextBlock Text="Questions" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            <Button Content="Add Q" Style="{StaticResource SmallPrimaryButton}" Command="{Binding AddQuestionCommand}" Margin="8,0"/>
                            <Button Content="Edit Q" Style="{StaticResource SmallPrimaryButton}" Command="{Binding EditQuestionCommand}" />
                            <Button Content="Del Q" Style="{StaticResource SmallPrimaryButton}" Command="{Binding DeleteQuestionCommand}" />
                            <Button Content="Import JSON" Style="{StaticResource SmallPrimaryButton}" Command="{Binding ImportQuestionFromJsonCommand}" />

                        </StackPanel>

                        <DataGrid Grid.Row="1" Style="{StaticResource CompactDataGrid}" 
                                  ItemsSource="{Binding ListeningQuestions}"
                                  SelectedItem="{Binding SelectedQuestion, Mode=TwoWay}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="#" Binding="{Binding QuestionNumber}" Width="50"/>
                                <DataGridTextColumn Header="Question" Binding="{Binding QuestionText}" Width="*"/>
                                <DataGridTextColumn Header="Correct" Binding="{Binding CorrectAnswer}" Width="90"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
